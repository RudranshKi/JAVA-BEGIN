package first_program.JFrame;
import javax.swing.*;
import java.awt.*;

public class jframe {
    public static void main(String[] args) {
      /*   JFrame frame = new JFrame("The pirate family");
        frame.setVisible(true);
        frame.setDefaultCloseOperation(frame.EXIT_ON_CLOSE); //important line since default close operation, doesn't exit the program
        frame.setSize(960,720);
        ImageIcon icon = new ImageIcon("crab.png");
        frame.setIconImage(icon.getImage());
        frame.getContentPane().setBackground(Color.BLACK);  //needs java.awt.* import for importing color method
*/
    MyFrame frame = new MyFrame();


    }
}
